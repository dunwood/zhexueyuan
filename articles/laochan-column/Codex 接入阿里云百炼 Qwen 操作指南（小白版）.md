# Codex 接入阿里云百炼 Qwen 操作指南（小白版）

> 发布日期: 2026-05-09

以下为老蝉实操，不需要记指令，也不需要懂执行它们的含义，你只要复制粘贴就行老蝉亲测可行。

![图片](/articles/images/Codex 接入阿里云百炼 Qwen 操作指南（小白版）/img_1.jpg)

![图片](/articles/images/Codex 接入阿里云百炼 Qwen 操作指南（小白版）/img_2.jpg)

适用场景：OpenAI Codex / Codex CLI 没有 ChatGPT 流量了，想临时或长期切换到阿里云百炼的 Qwen 模型继续用 Codex 做编程任务。本指南基于 Windows + PowerShell + VS Code + Codex CLI 环境整理。

### 0. 先说结论

Codex 可以通过配置文件接入阿里云百炼的 Qwen Responses 接口。

最终要改的是这个文件：

配置成功后，Codex 里会显示类似：

以后无论在 PowerShell 里用 Codex CLI，还是在 VS Code 里用 Codex 面板，都会优先读取这个配置。

### 1. 准备工作

你需要准备：

已经安装 Codex CLI；

已经安装 Node.js / npm；

已经有阿里云百炼 API Key；

Windows PowerShell；

VS Code，可选。

检查 Codex 是否可用：

如果 Codex 提示升级，建议升级：

或在 Codex 弹窗里选择：

### 2. 获取阿里云百炼 API Key

进入阿里云百炼控制台，创建 API Key。

注意：

如果已经不小心贴出来了，建议立刻去阿里云百炼后台删除旧 Key，重新生成一个新的。

### 3. 设置 Windows 环境变量

在 PowerShell 里执行：

例如：

### 引号要不要？

建议保留引号：

引号不会被保存进 Key，它只是告诉 PowerShell 这一整段是字符串。

### 4. 重要坑：setx 对当前窗口不生效

setx设置的是“以后新打开的 PowerShell 窗口”的环境变量。

所以你刚设置完，直接执行：

可能什么都看不到。

### 解决办法 A：重新打开 PowerShell

关闭当前 PowerShell，重新打开，再执行：

如果显示sk-...，说明成功。

### 解决办法 B：当前窗口临时设置

如果不想重开 PowerShell，可以在当前窗口执行：

然后检查：

### 5. 测试阿里云百炼接口是否通

先不要急着配置 Codex，先测试 API Key 和网络是否正常。

### 5.1 测试网络端口

看到：

说明网络能连上阿里云百炼服务器。

### 5.2 用 curl 测试模型接口

建议用 curl，并强制 IPv4：

如果返回：

说明：

### 常见状态码含义

### 6. PowerShell 乱码问题

如果你用中文输出，例如：

可能出现乱码：

不用管，换成英文输出即可：

### 7. 打开 Codex 配置文件

执行：

在你的电脑上，这个路径通常是：

### 8. 写入 Qwen 配置

如果你只想先跑通，推荐使用qwen3.6-plus。

把config.toml开头改成：

保存后关闭记事本。

### 9. 如果原配置文件里已有项目记录怎么办？

你可能原来已经有很多：

这些是 Codex 对项目目录的信任记录。

如果你熟悉 TOML，可以保留它们。

如果文件已经乱了，比如出现：

这种明显错误，建议直接重写成最小干净配置：

这样会删掉旧的项目授权记录，但不影响项目文件本身。下次进入项目时，Codex 会重新问你是否信任目录。

### 10. 检查配置文件

执行：

确认能看到：

### 11. 启动 Codex CLI

先进入具体项目目录，再启动 Codex。

例如：

进入后，如果问你是否信任目录：

选择：

一般直接按 Enter 就是第 1 项。

### 12. 判断是否接入成功

进入 Codex 后，顶部应该显示类似：

底部也可能显示：

这说明 Codex 已经用上阿里云百炼 Qwen 了。

可以测试一句：

如果回复：

说明已经跑通。

### 13. 重要区别：PowerShell 命令、Codex 自然语言、Codex slash 指令

这是今天最容易踩的坑。

你要分清楚三种输入环境：

### 13.1 看到这个，说明你在 PowerShell

这时先输入下面这句，进入你的项目目录：

进入后，提示行会变成类似：

然后根据你的目的，三选一：

注意：上面三条不是连续都要执行，而是三选一。

### 13.2 看到这个，说明你已经在 Codex 里面

这时不要再输入 PowerShell 命令，例如：

这些都不应该在›下面输入。

原因是：你已经进入 Codex 了。如果再输入codex resume --last，Codex 会把它当成一个“要执行的任务”，可能会出现“Codex 套 Codex”的嵌套执行问题。

### 13.3 进入 Codex 后，可以输入什么？

进入 Codex 后，主要输入自然语言任务，例如：

或者：

### 13.4 那进入 Codex 后还能不能用 / 指令？

可以。Codex CLI 进入›模式后，支持 slash commands，也就是以/开头的命令。

例如：

意思是：在 Codex 里面打开已保存会话列表，然后选择一个会话恢复。

注意它和 PowerShell 里的命令不是一回事：

这是 PowerShell 里的 Codex 子命令。

而：

这是 Codex 里面的 slash command。

### 13.5 最容易混淆的错误

错误理解：

这是错的。

在›下输入：

Codex 会把它当成普通自然语言/任务文本，而不是恢复命令。

正确做法有两个：

### 方式 A：还没进入 Codex，在 PowerShell 里恢复

### 方式 B：已经进入 Codex，在›下使用 slash 指令

然后从列表里选择要恢复的会话。

### 13.6 一句话记忆

### 14. 断线后如何重新进入 Codex

### 14.1 重新开一个新会话

在 PowerShell 里：

### 14.2 恢复最近一次会话

在 PowerShell 里：

注意：codex resume --last必须在 PowerShell 里输入，不是在 Codex 对话框里输入。

### 14.3 在 Codex 里面恢复会话

如果你已经看到：

说明已经在 Codex 里面。此时不要输入codex resume --last，可以尝试 slash command：

### 15. 如果恢复错了目录怎么办？

如果你在这里执行：

Codex 会恢复用户目录C:\Users\Admin下的会话，界面显示：

这不是你的项目目录。

正确做法是先进入项目目录：

### 16. 在 VS Code 里使用 Codex

不需要先在 PowerShell 里执行codex。

### 正确逻辑

二者是两个入口，不需要同时开。

### VS Code 使用方法

打开 VS Code，然后：

例如：

然后打开左侧 Codex 面板。

VS Code 里的 Codex 也会读取同一个配置：

所以模型仍然应该是：

### 17. 从 PowerShell 打开 VS Code

如果你已经确认要打开某个项目，可以执行：

但是要特别小心：

### 18. 保护原项目：强烈建议先复制副本

如果你原来的项目还能用，不要直接让 Codex 在原项目上改。

比如原项目是：

可以先复制一个测试副本：

然后只在副本里让 Codex 修改。

### 19. 让 Codex 先读项目，不要马上改

进入 Codex 后，先让它只读：

确认它读的是正确目录，再让它给方案：

最后才让它执行：

### 20. 复杂任务要拆开

不要一次让 Codex 同时改数据库、GUI、测试、文档。

推荐拆成：

这样更安全，也更容易回滚。

### 21. 让 Codex 停止当前任务

如果 Codex 一直运行，或者你发现目录不对：

先按：

如果还不停，再按：

然后让它只检查状态：

### 22. 检查项目是否被改动

如果项目是 Git 仓库，执行：

如果不是 Git 仓库，可以查最近修改文件：

重点看有没有：

### 23. 切换到 qwen3-coder-plus

确认qwen3.6-plus能正常工作后，可以尝试代码模型。

打开配置文件：

把：

改成：

保存后重新启动 Codex。

如果不稳定，再切回：

### 24. 切回 OpenAI / ChatGPT 大模型

打开配置文件：

把 Qwen 配置改成最简 OpenAI 配置：

保存后重新启动 Codex。

如果你的账号暂时没有gpt-5.5，可以换成你能用的 OpenAI Codex 模型。

### 25. 建议做两个配置文件，一键切换

可以准备两个文件：

### 切到 Qwen

### 切到 OpenAI

切换后重新打开 Codex。

### 26. 今天踩过的坑总结

### 坑 1：API Key 设置了但 echo 为空

原因：

解决：

或重新打开 PowerShell。

### 坑 2：Invoke-RestMethod 一直卡住

可以换成：

### 坑 3：中文 curl 输出乱码

换成英文输出：

### 坑 4：进入 Codex 后误以为 resume --last 可以恢复

看到：

说明你已经在 Codex 里面。

这时不要输入：

正确区分：

如果你在›下输入codex resume --last，就会变成“让 Codex 去执行 Codex”，容易卡住或嵌套。

如果你在›下输入resume --last，它也不会等同于恢复命令，只会被当成普通输入。

### 坑 5：在错误目录恢复会话

如果在：

执行：

恢复的是用户目录，不是项目目录。

正确：

### 坑 6：误让 Codex 在原项目里改代码

如果原项目已经能用，先复制副本：

然后在副本里改。

### 坑 7：config.toml 被写乱

如果出现明显残缺内容，直接重写最小配置：

### 27. 最小成功流程

如果只想照抄最短流程：

重新打开 PowerShell，或当前窗口执行：

测试：

写入配置：

进入项目：

测试：

看到模型显示：

就成功了。

### 28. 最后提醒

不要把 API Key 发给别人；

不要在原项目上直接做大改；

看到PS才能输入 PowerShell 命令；

看到›就只输入自然语言任务；

Codex 进入项目后，先让它只读项目结构，再让它给方案，最后才让它改代码；

复杂任务一定拆阶段执行。